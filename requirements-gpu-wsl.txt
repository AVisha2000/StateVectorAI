# Optional WSL GPU profile installed only after a CUDA-enabled JAX wheel.
# Deliberately excludes jax and jaxlib so pip cannot replace the CUDA wheel.
PyYAML==6.0.3
datasets==5.0.0
fastapi==0.138.0
flax==0.12.7
hypothesis==6.155.2
httpx==0.28.1
matplotlib==3.10.8
mlflow-skinny==3.13.0
mlflow-tracing==3.13.0
mlflow==3.13.0
numpy==2.4.4
optax==0.2.8
orbax-checkpoint==0.10.3
pennylane==0.45.0
pennylane_lightning==0.45.0
pytest==9.0.3
pyyaml_env_tag==1.1
uvicorn==0.49.0
